# CUDA-enabled PyTorch (for GPU inference with CUDA 12.1)
--extra-index-url https://download.pytorch.org/whl/cu121

# Core dependencies
flask>=3.0.0
waitress>=3.0.0
pocket-tts>=1.1.0

# Audio processing
torch>=2.0.0,<=2.8.0
torchaudio>=2.0.0,<=2.8.0
scipy>=1.10.0
numpy>=1.24.0
soundfile>=0.12.0
