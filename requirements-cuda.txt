# CUDA-enabled PyTorch (for GPU inference with CUDA 11.8)
# Using cu118 wheels to support Pascal-generation GPUs (GTX 1060/1070/1080,
# Titan X/Xp, Tesla P100, etc.) which have CUDA capability sm_61/sm_62.
# PyTorch 2.5+ dropped Pascal support; this pins to the last compatible release.
--extra-index-url https://download.pytorch.org/whl/cu118

# Core dependencies
flask>=3.0.0
waitress>=3.0.0
pocket-tts>=1.1.0

# Audio processing — capped at 2.4.1: last release with Pascal (sm_61) support
torch>=2.0.0,<=2.4.1
torchaudio>=2.0.0,<=2.4.1
scipy>=1.10.0
numpy>=1.24.0
soundfile>=0.12.0
